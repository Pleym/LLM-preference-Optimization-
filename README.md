# LLM-preference-Optimization


The goal of this project is to propose a true analyse of the optimization method for adapt
language models with human preferences.
SFT uses labeled data to train the model.
DPO uses pariwise data to learn.

